source("renv/activate.R")
options(rmarkdown.html_dependency.header_attr = FALSE)

Sys.setenv(PATH = paste("/Users/jpunyon/go/bin", Sys.getenv("PATH"), sep = ":"))

# fix Hugo version
options(blogdown.hugo.version = "0.117.0")
